// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2013.3
// Copyright (C) 2013 Xilinx Inc. All rights reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xgray_scale.h"

extern XGray_scale_Config XGray_scale_ConfigTable[];

XGray_scale_Config *XGray_scale_LookupConfig(u16 DeviceId) {
	XGray_scale_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XGRAY_SCALE_NUM_INSTANCES; Index++) {
		if (XGray_scale_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XGray_scale_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XGray_scale_Initialize(XGray_scale *InstancePtr, u16 DeviceId) {
	XGray_scale_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XGray_scale_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XGray_scale_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

