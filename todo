-- try out with slow IPs, 1 cpu configuration but having 2 HAs for grayscale only
-- use only grayscale processing
-- see if one HA is busy processing previous frame then current frame can be handled in 2nd HA??
